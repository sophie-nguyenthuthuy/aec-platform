{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "mkdir -p public && echo '<!doctype html><title>AEC Platform API</title><p>AEC Platform API — see <a href=\"/api/v1/healthz\">/api/v1/healthz</a></p>' > public/index.html",
  "installCommand": "echo 'no install at project level — api/requirements.txt is installed per-function'",
  "outputDirectory": "public",
  "functions": {
    "api/index.py": {
      "runtime": "@vercel/python@4.3.0",
      "maxDuration": 60,
      "includeFiles": "apps/api/**/*.py,apps/ml/**/*.py,apps/api/alembic/**/*"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
