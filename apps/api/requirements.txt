fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3
asyncpg==0.29.0
psycopg2-binary==2.9.9
pydantic==2.13.3
pydantic-settings==2.5.2
pyjwt==2.9.0
httpx==0.27.2
redis==5.1.0
celery==5.4.0
arq==0.26.1
boto3==1.35.36
aioboto3==13.2.0
python-multipart==0.0.12
Pillow==10.4.0
weasyprint==62.3
email-validator==2.2.0
pgvector==0.3.5
python-dotenv==1.2.2
# cachetools — used by ml.pipelines.codeguard for memoizing JWKS-style
# lookups; pinned here (not via a transitive dep) so the lock pin is stable
# across langchain bumps.
cachetools==5.5.0
# Sentry — `core.observability.init_sentry` imports this lazily, but
# the lazy import only succeeds in prod if the package is actually
# installed. Without this pin, prod deploys would silently fall
# through to the `ImportError` branch and never report errors;
# `tests/test_observability.py::test_init_sentry_is_noop_without_dsn`
# wouldn't catch it because the test path hits the no-DSN branch
# first.
sentry-sdk[fastapi]==2.18.0
# AI
langchain==0.3.1
langgraph==0.2.28
langchain-anthropic==0.2.1
langchain-openai==0.2.0
anthropic==0.39.0
openai==1.51.0

# Price-bulletin parsing — many provincial DOCs publish only as DOCX/PDF.
# python-docx: table extraction from Word documents (MIT-licensed, pure Python).
# pdfplumber: table extraction from PDFs (MIT, wraps pdfminer.six).
python-docx==1.1.2
pdfplumber==0.11.4

# BOQ I/O — Excel import/export and PDF export for `services.boq_io`.
# openpyxl: pure-Python .xlsx read/write (no LibreOffice / system deps).
# reportlab: pure-Python PDF generation. Used for BOQ exports because a
#   tabular report doesn't need the full CSS pipeline weasyprint pulls
#   in via cairo/pango.
openpyxl==3.1.5
reportlab==4.2.5

# HTML parsing for BIDRADAR scrapers (mua-sam-cong.gov.vn, philgeps,
# egp.go.th, etc.). Required at *import* time by apps/ml/pipelines/bidradar.py,
# so any test touching that lane pulls it in transitively — without this
# CI fails with `ModuleNotFoundError: bs4`.
beautifulsoup4==4.12.3
