-r requirements.txt

# Pinned: an unpinned `pip install ruff` lets CI drift to whatever release
# happens to be latest, and new lint rules silently break the gate. Bump
# this deliberately when you want to adopt new rules.
ruff==0.15.12
mypy==1.13.0

pytest==8.3.3
pytest-asyncio==0.24.0
# pytest-cov produces line + branch coverage. We pin a known-good
# version; major bumps occasionally change the XML/JSON report shape
# that CI artifact consumers depend on, which would silently red CI.
pytest-cov==6.0.0
# pytest-timeout: per-test timeout that interrupts a single hung test
# rather than letting the whole suite block until the GitHub-hosted
# runner is preempted. Two consecutive CI runs on `feat/ai-assistant-rbac`
# died at the runner level (one "shutdown signal" at 11min, one "lost
# communication" at 52min) without producing test output — strong
# signal that a test was hanging long enough to exhaust the runner's
# resources. Set `--timeout=120 --timeout-method=thread` on the CI
# invocation; thread-based timeout is asyncio-safe (signal-based isn't).
pytest-timeout==2.4.0
httpx==0.27.2
