export default function PulseIndexPage() {
  return (
    <div className="mx-auto w-full max-w-4xl p-4">
      <h1 className="text-xl font-semibold">ProjectPulse</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Select a project to view its dashboard, tasks, schedule, change orders,
        meetings, and client reports.
      </p>
    </div>
  );
}
